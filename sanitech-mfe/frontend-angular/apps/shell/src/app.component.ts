import { Component } from '@angular/core';
@Component({
  selector: 'app-root',
  standalone: true,
  template: `<h1>Clinica Guastamacchia — Angular Shell (MF)</h1>
  <p>Predisposizione MF e OIDC (da completare con issuer/clientId e remoti).`,
})
export class AppComponent {}
