FROM postgres:16

# -----------------------------------------------------
# Immagine Postgres dedicata al microservizio
# -----------------------------------------------------
# Base ufficiale Postgres, gestita con build locale per
# allineare il flusso con gli altri servizi infrastrutturali.
