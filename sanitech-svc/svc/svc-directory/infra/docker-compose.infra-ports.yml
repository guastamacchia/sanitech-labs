services:
  postgres:
    ports:
      - "5432:5432"
  kafka:
    ports:
      - "29092:29092"
  keycloak:
    ports:
      - "8081:8080"
  prometheus:
    ports:
      - "9090:9090"
  grafana:
    ports:
      - "3000:3000"
