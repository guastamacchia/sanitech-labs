# Config minimale LiveKit per ambiente di sviluppo.
# In produzione usare configurazioni certificate (TURN, TLS, Redis, ecc.).

port: 7880

rtc:
  tcp_port: 7881
  udp_port: 7882

# API keys (dev). In produzione gestire le chiavi tramite secret manager.
keys:
  devkey: devsecretdevsecretdevsecretdevsecret
