{{- if .Values.networkPolicy.enabled }}
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "svc-consents.fullname" . }}
spec:
  podSelector:
    matchLabels:
      app: {{ include "svc-consents.fullname" . }}
  policyTypes:
    - Ingress
    - Egress
  ingress:
    - from:
        - podSelector: {} # personalizzare in base a gateway/namespace
  egress:
    - {} # consente egress verso DB/Kafka/DNS (tuning consigliato in prod)
{{- end }}
