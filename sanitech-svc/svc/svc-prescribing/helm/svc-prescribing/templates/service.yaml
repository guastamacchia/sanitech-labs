apiVersion: v1
kind: Service
metadata:
  name: {{ include "svc-prescribing.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "svc-prescribing.fullname" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
