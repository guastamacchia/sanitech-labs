package it.sanitech.scheduling.repositories.entities;

/**
 * Modalità della prestazione.
 */
public enum VisitMode {
    /** Visita in presenza (in sede). */
    IN_PERSON,
    /** Visita da remoto (video-consulto). */
    TELEVISIT
}
